Réalisation :

Partie NodeJS : Auriles El Haddad

Installation de MEAN Stack : 
sudo npm install -g generator-meanstack

Installation de Yeoman : 
npm install --global yo bower grunt-cli

Entrer dans le dossier du projet :
../NodeJS/app

Puis lancer la commande :
yo meanstack

Abandon de la MEAN stack ( problème rencontré )

Création des charts graphiques avec GoogleCharts

Mise en lien sur une même page des jauges et du graphique

Entrer dans le dossier du projet :
cd ../NodeJS/app

npm install 

npm install express

npm install jade 

création de app.js

installer les dépendances manquantes :
npm install express-session
npm install body-parser
npm install cookie-parser
npm install mongoose
npm install path

création du MVC ( models, views, controllers )

création des routes

lancer le serveur mongo via la commande :
sudo mongod

ouvrir une seconde fenetre en terminal et lancer la commande :
node app.js 

ouvrir le navigateur, entrez l'ip : http://127.0.0.1:3006/ 

création du PowerPoint


Problèmes rencontrés lors du projet : 

- Organisation des membres de l'équipe
- Tâches non effectuées
- Difficultés de couplage entre les deux chartes graphiques de GoogleCharts
- Programme semi-fonctionnel
- Problème lors de l'utilisation de la MEAN Stack ( MongoDB, Express, AngularJS, NodeJS ) 
- Problème lors de l'intégration de l'objet 
- Problème lors du chargement des graphiques 
( Error: Failed to lookup view "charts-gauge" in views directory "/Users/Auriles/.Trash/app 22.57.39/app/views" )

